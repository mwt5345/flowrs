[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyflowrs"
version = "0.1.0"
requires-python = ">=3.9"
description = "Python bindings for flowrs normalizing flows (Rust + Burn)"

[tool.maturin]
features = ["pyo3/extension-module"]
